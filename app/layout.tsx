import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SAO RPG Dice Roller | Adventure Awaits",
  description: "Roll dice for your Sword Art Online RPG adventures. Track your character's fate with BD, CD, LD, and MD dice rolls.",
  keywords: "SAO, RPG, dice roller, Sword Art Online, tabletop gaming, character rolls",
  authors: [{ name: "SAO RPG Community" }],
  viewport: "width=device-width, initial-scale=1, maximum-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
